# YASB Widget Configuration
# Copy these widget definitions into your YASB config.yaml

# ━━━ OPTION 1: Grouped widget (RECOMMENDED) ━━━━━━━━━━━━━━━━━━━━
# A collapsible container showing all 5 platforms with accent colors.
# Left-click toggles compact/detailed. Right-click opens dashboard.
#
# Add ONLY "ai_usage_group" to your bar widgets:
#   widgets_right: ["ai_usage_group", "volume", "clock"]

  ai_usage_group:
    type: "yasb.grouper.GrouperWidget"
    options:
      class_name: "ai-usage-group"
      widgets: ["ai_chatgpt", "ai_copilot", "ai_claude", "ai_ollama", "ai_zai", "ai_gemini", "ai_status"]

  ai_chatgpt:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\udb80\udcab</span> {data[chatgpt_stale_marker]}{data[chatgpt_display]}"
      label_alt: "<span>\udb80\udcab</span> {data[chatgpt_stale_marker]}{data[chatgpt_display_alt]}"
      class_name: "ai-chatgpt"
      label_placeholder: "\udb80\udcab --"
      tooltip: true
      tooltip_label: "{data[chatgpt_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/chatgpt"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"

  ai_copilot:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\uf113</span> {data[copilot_stale_marker]}{data[copilot_display]}"
      label_alt: "<span>\uf113</span> {data[copilot_stale_marker]}{data[copilot_display_alt]}"
      class_name: "ai-copilot"
      label_placeholder: "\uf113 --"
      tooltip: true
      tooltip_label: "{data[copilot_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/copilot"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"

  ai_claude:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\udb81\ude2e</span> {data[claude_stale_marker]}{data[claude_display]}"
      label_alt: "<span>\udb81\ude2e</span> {data[claude_stale_marker]}{data[claude_display_alt]}"
      class_name: "ai-claude"
      label_placeholder: "\udb81\ude2e --"
      tooltip: true
      tooltip_label: "{data[claude_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/claude"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"

  ai_ollama:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\uf201</span> {data[ollama_stale_marker]}{data[ollama_display]}"
      label_alt: "<span>\uf201</span> {data[ollama_stale_marker]}{data[ollama_display_alt]}"
      class_name: "ai-ollama"
      label_placeholder: "\uf201 --"
      tooltip: true
      tooltip_label: "{data[ollama_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/ollama"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"

  ai_zai:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\udb80\udea8</span> {data[zai_stale_marker]}{data[zai_display]}"
      label_alt: "<span>\udb80\udea8</span> {data[zai_stale_marker]}{data[zai_display_alt]}"
      class_name: "ai-zai"
      label_placeholder: "\udb80\udea8 --"
      tooltip: true
      tooltip_label: "{data[zai_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/zai"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"


  ai_gemini:
    type: "yasb.custom.CustomWidget"
    options:
      label: "<span>\uf1a0</span> {data[gemini_stale_marker]}{data[gemini_display]}"
      label_alt: "<span>\uf1a0</span> {data[gemini_stale_marker]}{data[gemini_display_alt]}"
      class_name: "ai-gemini"
      label_placeholder: "\uf1a0 --"
      tooltip: true
      tooltip_label: "{data[gemini_tooltip]}"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 5 http://localhost:3456/api/yasb/gemini"
        run_interval: 120000
        return_format: "json"
        hide_empty: true
      callbacks:
        on_left: "toggle_label"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"

  ai_status:
    type: "yasb.custom.CustomWidget"
    options:
      label: "{data[indicator]} {data[next_label]}"
      label_alt: "{data[indicator]} {data[next_label]}"
      class_name: "ai-status"
      label_placeholder: "\uf10c"
      tooltip: true
      tooltip_label: "AI Usage Dashboard\nStatus: Online\nNext refresh in: {data[next_s]}s\nLeft-click: open dashboard\nRight-click: refresh data"
      exec_options:
        run_cmd: "curl.exe -sf --max-time 2 http://localhost:3456/api/health"
        run_interval: 5000
        return_format: "json"
        hide_empty: false
      callbacks:
        on_left: "exec cmd /c start http://localhost:3456"
        on_middle: "do_nothing"
        on_right: "exec curl.exe -sf http://localhost:3456/api/yasb/refresh"


# ━━━ OPTION 2: Single combined widget (simpler) ━━━━━━━━━━━━━━━━
# One compact widget showing all platforms at once.
# Add "ai_usage" to your bar widgets list instead of "ai_usage_group".

#  ai_usage:
#    type: "yasb.custom.CustomWidget"
#    options:
#      label: "<span>\udb80\udcab</span> GPT {data[chatgpt_session]}% | Cop {data[copilot_pct]}% | Cla {data[claude_session]}% | Oll {data[ollama_session]}% | Z {data[zai_token_pct]}%"
#      label_alt: "<span>\udb80\udcab</span> GPT {data[chatgpt_session]}%({data[chatgpt_limit_reset]})/{data[chatgpt_weekly]}%({data[chatgpt_weekly_reset]}) | Cop {data[copilot_used]}/{data[copilot_total]} ({data[copilot_reset]}) | Cla {data[claude_session]}%({data[claude_limit_reset]})/{data[claude_weekly]}%({data[claude_weekly_reset]}) | Oll {data[ollama_session]}%({data[ollama_limit_reset]})/{data[ollama_weekly]}%({data[ollama_weekly_reset]}) | Z Tk{data[zai_token_pct]}%/MCP{data[zai_mcp_pct]}%"
#      class_name: "ai-usage-combined"
#      label_placeholder: "\udb80\udcab Loading..."
#      tooltip: true
#      tooltip_label: "AI Command Center\nChatGPT: {data[chatgpt_session]}% session ({data[chatgpt_limit_reset]}) / {data[chatgpt_weekly]}% weekly ({data[chatgpt_weekly_reset]})\nCopilot: {data[copilot_used]}/{data[copilot_total]} premium ({data[copilot_reset]})\nClaude: {data[claude_session]}% session ({data[claude_limit_reset]}) / {data[claude_weekly]}% weekly ({data[claude_weekly_reset]})\nOllama: {data[ollama_session]}% session ({data[ollama_limit_reset]}) / {data[ollama_weekly]}% weekly ({data[ollama_weekly_reset]})\nZ.AI: Token {data[zai_token_pct]}% / MCP {data[zai_mcp_pct]}%"
#      exec_options:
#        run_cmd: "curl.exe -sf --max-time 2 http://localhost:3456/api/yasb/summary"
#        run_interval: 120000
#        return_format: "json"
#        hide_empty: false
#      callbacks:
#        on_left: "toggle_label"
#        on_middle: "do_nothing"
#        on_right: "exec cmd /c start http://localhost:3456"
#      label_shadow:
#        enabled: true
#        color: "black"
#        radius: 3
#        offset: [1, 1]
